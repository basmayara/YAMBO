{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "ConnectionStrings": {
    // ⚠️ Secret: moved to environment variable or appsettings.Development.json
    // In production → set env var: ConnectionStrings__DefaultConnection
    "DefaultConnection": ""
  },
  "EmailSettings": {
    // ⚠️ Secret: moved to environment variable or appsettings.Development.json
    // In production → set env var: EmailSettings__FromEmail / EmailSettings__AppPassword
    "FromEmail": "",
    "AppPassword": ""
  },
  // ⚠️ Secret: moved to environment variable or appsettings.Development.json
  // In production → set env var: JwtSecret
  "JwtSecret": "",
  "AllowedHosts": "*"
}